RabbitMQ的Worker模式
	worker模式其实就是一对多模式：
		默认情况下，RabbitMQ会顺序的将message发给下一个消费者。每个消费者会得到平均				数量的message。这种方式称之为round-robin（轮询).
		但是很多情况下并不希望消息平均分配,而是要消费快的多消费,消费少的少消费.还有很多				情况下一旦其中一个宕机,那么另外接收者的无法接收原本这个接收者所要接收的数据。
	Exchange模式
		在RabbitMQ在Exchange模式中生产者并不会直接把消息发送到Exchange（交换机）,
		消费者创建各自的队列绑定到交换机